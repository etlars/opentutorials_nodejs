doctype html
html
  head
    meta(charset='utf-8')
  body
    h2
      a(href='/topic') Server Side JavaScript with OrientDB
    ul
      each topic in topics
        li
          - rid = encodeURIComponent(topic['@rid'])
          a(href='/topic/'+rid)=topic.title
    article
      if topic
        h2=topic.title
        = topic.description
      else
        h2 Welcome
        |  This is server side javascript tutorial.
    p
      div
        a(href='/topic/new') new
